================================================================================
REVYN – DETAILED UI/UX FIX LIST (Current Version)
Precise spacing, positioning, alignment and interaction fixes
================================================================================

1. SIDEBAR
----------
- Logo + “Revyn” text: increase size slightly. Logo should be 28–32px, text 16–17px bold.
- Reduce the gap between logo block and “Policy Engine Active” badge to 12px.
- Active nav item (Dashboard): add a 3px solid primary-blue left border and increase background opacity slightly.
- Nav items vertical spacing: set consistent 4px gap between each item (currently slightly uneven).
- Bottom of sidebar: add 16px padding from the bottom edge. Remove any leftover track/buildathon text if still present.
- “Razorpay Test Mode Only” footer text: make it 11px and reduce opacity to 60%.

2. TOP HEADER AREA
------------------
- Page title “Revenue Recovery Overview”: keep current size but reduce subtitle opacity to 70% and set subtitle margin-top to 4px.
- Status pills on the right (TEST MODE + Guardrails OK):
  - Make both pills same height (28px).
  - Add 8px gap between the two pills.
  - Align them vertically center with the title block.
- Overall header bottom margin: increase to 24px before the metric cards.

3. METRIC CARDS (TOP ROW)
-------------------------
- Card height: force all four cards to exactly the same height (currently slightly uneven).
- Internal padding: set to 20px all sides.
- Gap between the four cards: exactly 16px.
- Icon size inside cards: 18–20px, place 12px from top and 16px from right.
- Large number: keep bold, reduce letter-spacing by 0.5px for tighter look.
- Secondary text under number: 12px, margin-top 6px.
- “REVENUE AT RISK” label: make all four labels the same font-size and weight.

4. EXECUTE RECOVERY PIPELINE BLOCK
----------------------------------
- Top margin from metric cards: 24px.
- Internal padding: 20px 24px.
- Left icon + title: align vertically center, 12px gap between icon and title.
- Description text: max-width 520px, line-height 1.5, opacity 75%.
- Right side controls (Batch dropdown + Run button + Trash):
  - Align all three items vertically center.
  - Gap between dropdown and Run button: 12px.
  - Gap between Run button and Trash icon: 8px.
- Run Batch Recovery button: height 36–40px, horizontal padding 20px.
- Add a subtle hover lift (translateY -1px) and transition 150ms ease.

5. RECENT PAYMENT CASES SECTION
-------------------------------
- Section title + count: margin-bottom 16px.
- Filter tabs row:
  - Height of each tab pill: 28px.
  - Horizontal gap between tabs: 8px.
  - Active tab: stronger background + 1px border in primary color.
- Search input:
  - Height 36px.
  - Padding-left 36px (for search icon).
  - Border-radius 8px.
  - Focus ring: 2px primary blue with 20% opacity.
- Table header:
  - Background slightly lighter than body (#1F2937).
  - Padding top/bottom 12px.
  - Font-size 11px, uppercase, letter-spacing 0.5px, opacity 70%.
- Table body rows:
  - Height 52–56px.
  - Vertical padding 10px.
  - Hover background: very subtle (#1F2937 at 40% opacity).
  - Transition on hover: 120ms ease.
- Status badges: consistent height 22px, padding 0 8px, border-radius 6px.
- “Inspect →” button: make it a ghost button with consistent padding and hover state.

6. RECOVERIES PAGE (WORKSPACE)
------------------------------
- Top summary cards (Total / Recovered / Escalated / Unrecoverable):
  - Make all four the same height.
  - Gap between them: 16px.
  - Internal padding 16px 20px.
- “All Payment Cases” title row: margin-top 24px, margin-bottom 12px.
- Table styling: match exactly the Dashboard table (same row height, badge style, hover).

7. AUDIT TRAIL PAGE
-------------------
- “Compliance-Grade Audit Standard” info card:
  - Padding 16px 20px.
  - Margin-bottom 20px.
- Step filter tabs: same style and spacing as the cases filter tabs.
- Table:
  - STEP column badges: keep color coding but make width consistent.
  - Reason column: allow 2-line clamp with tooltip on hover.
  - Expandable chevron: 16px size, rotate 90deg on open with 150ms transition.
- Total Entries counter: align right, font-weight 600.

8. SETTINGS PAGE
----------------
- Kill Switch card:
  - Increase internal padding to 20px 24px.
  - Status text (“System operational…”) should sit 8px below the description.
  - “Activate Kill Switch” button: height 36px, align to the right side of the card.
- Policy rules grid:
  - 2×2 grid with 16px gap.
  - Each rule card padding 16px.
  - Title and value should have 6px vertical gap.
- Integration health cards:
  - Three cards in a row with 16px gap.
  - Equal height.
  - Status badge (Ready / Connected) aligned to the top-right of each card.

9. GLOBAL SPACING & RHYTHM
--------------------------
- Main content left padding (after sidebar): 28–32px.
- Main content right padding: 28–32px.
- Maximum content width: keep current but ensure consistent side padding.
- Vertical section spacing throughout the app: use 24px or 32px only (avoid random 18px / 22px values).

10. TRANSITIONS & MICRO-INTERACTIONS
------------------------------------
- All buttons: transition 150ms cubic-bezier(0.4, 0, 0.2, 1).
- Cards hover (optional): very subtle scale(1.01) or border-color change, 150ms.
- Sidebar active state: transition background and border 150ms.
- Drawer / modal open: 200ms ease-out.
- Toast / success banners: slide down + fade, 200ms.

11. MOBILE FIXES
----------------
- Metric cards: stack with 12px gap, full width.
- Execute Pipeline block: stack the controls vertically with 12px gap.
- Table: enable horizontal scroll with padding-bottom 8px for the scrollbar.
- Sidebar: collapse to hamburger, overlay with 40% black backdrop.
- Top status pills: move under the title or into a compact row.

12. FINAL POLISH
----------------
- Favicon: confirm new logo is used (not lightning).
- All icons: use consistent stroke width (1.5px or 2px).
- Scrollbars: dark theme styled (thin, rounded, low contrast).
- Selection color: primary blue at 30% opacity.
- Make sure no element is closer than 8px to another interactive element.

================================================================================
END OF DETAILED UI/UX FIX LIST
================================================================================